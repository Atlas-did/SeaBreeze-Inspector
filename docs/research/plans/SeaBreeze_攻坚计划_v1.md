# SeaBreeze-Inspector 攻坚计划 v1

> **日期**：2026-08-19 · **硬截止**：2026-09-15 · **目标**：评审口径「85 分及格」
> **本计划的输入**：四个交付包（训练结果_v4 / 论文文档 / 数据集清单 / 证据脚本与输出）+ GitHub 仓库全量核对
> **核心判断**：检测主链（A 轨道）已打通（v4 零泄漏 mAP50=0.674），但**论文、数据清单、证据脚本三者尚未闭环**，且存在多处会直接砸论文的数字红线。26 天里先止血、再补证据、最后整合。

---

## 0. 一页现状盘点

| 轨道 | 当前状态 | 一句话 |
|---|---|---|
| **A 检测** | 🟢 主链已通 | v4 = yolo11s@1024、零泄漏、clean 核实；mAP50 **0.674**（峰值）/ 0.668（最佳检查点） |
| **论文** | 🔴 严重脱节 | main.tex 三处引用泄漏版 v3 废数（0.58），架构名还是 YOLOv8-Nano，v4 结果完全没进论文 |
| **数据纪律** | 🟡 分裂 | 仓库提交的是**泄漏版** split_manifest（val 90.8% 泄漏）；干净的 clean_manifest 没进仓库；v4 的 DTU 数据集**无 manifest** |
| **B 抗风** | 🔴 反例 | B2 力平衡：设计假设参数下 vmax≈8.2 m/s，6 级风（12m/s）正裕度 **0.0%**——当前证据不支持"6 级抗风" |
| **B1 EKF** | 🔴 无区分度 | 三档 Q RMSE = 0.798/0.799/0.800 几乎相同；静风基线 d_hat std=0.109（非≈0） |
| **C 机械臂** | 🟡 尾部有坑 | FK/IK P50=2.7e-10mm 好看，但 **P95=123mm**、ikfk P95=3465°，尾部数值灾难 |
| **D 可靠** | 🟡 半模拟 | D1/D3 ✅；D2 时延是纯 np.random 模拟 |
| **硬指标自评** | ~10/35 → 目标 ~20/35 | 检测二分类 + 力平衡外推 + 悬停风扇实测 + 论文重构四件事可拉到 ~20 |

---

## 1. 核心问题清单（每条都有实锤位置）

### P0 — 会直接砸论文的（今天起 2 天内做完）

| # | 问题 | 实锤位置 | 后果 |
|---|---|---|---|
| P0-1 | **泄漏版 v3 废数 0.58 进了论文三处**（摘要 / 方法 / 局限性），违反你自己的 GAP_TRACKER 红线「旧 v3 的 mAP50=0.58 是 83% 泄漏的废数，不能引用」 | main.tex **L62**（摘要）、**L498–499**（§4.5 方法）、**L717**（§6.2.4 局限性）；项目总结报告 §3.5/§6.2/§9/§15/§16/§18 同样引用 | 审稿人一旦用 sha256 复测 val（90.8% 泄漏），整篇检测结论崩塌 |
| P0-2 | **架构名错误**：论文全篇「YOLOv8-Nano」，实际训练的是 **YOLO11s** | args.yaml `model: yolo11s.pt`；main.tex L57/L62/§4.5 | 方法节写错架构 = 硬伤（GAP_TRACKER 全局坑 #7） |
| P0-3 | **任务口径矛盾**：摘要/方法说「裂纹/腐蚀/前缘损伤三类」，实际 v4 训练的是 **nc=1 二分类 defect** | main.tex L57、L496；v4_experiment_report.md `Single-class object detection (nc=1)` | 类别数对不上，混淆矩阵图也是二分类的 |
| P0-4 | **论文实验节完全没有检测实验**：§5 只有 EKF/控制/RRT*/端到端，检测精度（论文最硬的数字）没有章节支撑 | main.tex §5（L560–683） | v4 的 0.674 无处安放 |
| P0-5 | **v4 四张图是孤儿**：boxpr_curve / confusion_matrix / dataset_split / cost_comparison 在 figures/ 里但没被 `\includegraphics` 引用 | main.tex 全部 7 处 includegraphics vs figures/ 11 张 | 图白做了 |

### P0 — 数字红线（写进论文前必须先定）

| # | 问题 | 实锤 | 处理 |
|---|---|---|---|
| P0-6 | **00_说明 的头部数字是混搭**：「0.674/0.348/0.795/0.664」把 mAP50 峰值 epoch(190) 的 R 和最佳检查点 epoch(182) 的 P 拼在一起 | 训练结果_v4/00_说明 vs v4_experiment_report.md §2 | **统一用最佳检查点（ep182）：0.668 / 0.348 / 0.795 / 0.619**，峰值单独注明「mAP50 峰值 0.674 @ep190」 |
| P0-7 | **「6 级抗风」措辞与 B2 反例冲突** | wind_resistance_report.json `p_ok_12ms: 0.0` | 无实测参数前禁止写「6 级」；统一写「设计假设下 vmax≈8 m/s」 |
| P0-8 | **FK/IK「<1e-9 m」掩盖尾部** | fk_ik_report.json P95=123mm、ikfk P95=3465° | 论文写 P50 时必须同时报 P95/P99 并解释（奇异位形/边界附近优化残差） |

### P1 — 数据纪律（3 天内）

| # | 问题 | 实锤 | 处理 |
|---|---|---|---|
| P1-1 | **仓库 `data/split_manifest.csv` 是泄漏版**：15528 行但仅 7001 唯一内容，val 90.8% 内容在 train 出现 | 实测：`len(val∩train)=2071/2280` | 用 `clean_manifest.csv`（7001 唯一、train 4900/val 1050/test 1051、**三向零碰撞已实测**）替换/并存，旧文件改名 `split_manifest_ILIAD_leaky.csv` 或删除 |
| P1-2 | **v4 的 DTU 数据集无 manifest**：clean_binary_v4_quick（3040 瓦片）只在 00_说明/args.yaml/报告里出现，逐图 sha256 没有 | 数据集清单/00_说明 L5 自认「不在本包内」 | 补建 `data/dtu_v4_manifest.csv`（逐瓦片 sha256 + parent 图 + split），这是答辩最值得拿出手的证据 |
| P1-3 | **kimi_screen_full.csv 只有 40 张**，而说明声称「45% 未标注图其实有缺陷」需要更大样本 | kimi_screen_full.csv 41 行 | 补到 ≥100 张再下统计结论；把筛查 CSV 纳入 v4 数据集文档 |

### P1 — 证据脚本改造（4–10 天，可与 GPU/硬件并行）

| # | 模块 | 问题 | 改造方案 |
|---|---|---|---|
| P1-4 | **B1 EKF 消融** | 三档 Q RMSE 几乎相同 → 消融无区分度；静风基线非≈0 | ① Q 跨数量级扫（0.05/0.5/5）；② 同时扫 R；③ 改报告时变扰动下的**跟踪延迟**（超调/收敛时间）而非只看 RMSE；④ 静风残差来源写进论文讨论（先验 Q 偏大 → d_hat 有偏） |
| P1-5 | **B2 抗风** | 参数是「设计假设非实测」 | ① 等真实平台参数重跑（阻塞项：用户提供 x500/Tello 实测质量/推力/倾角/CdA）；② 论文降级措辞；③ **修脚本 bug**：`print` 的"敏感性排序"用的是未排序 dict，与报告 JSON 排序不一致（C_D_Aeff 才是第一敏感项） |
| P1-6 | **D2 时延** | 纯 np.random | 实机插桩（Tello SDK 时间戳 / Arduino 串口回环），报 t0–t3 分解 |
| P1-7 | **A3 雾方案** | consistency_train / dehaze_joint 是接口骨架 | 按抗雾方案文档 §12 修正后的方案 B 实现（检测任务删 L_cons 只留 L_feat；防塌缩用 stop-gradient；P3/P4/P5 多尺度投影头） |
| P1-8 | **A2 死参数** | train_cli.py `--cls_pw` 解析了没接进 train()，且 ultralytics 无此参数 | 删掉，改用 `fl_gamma` / class_weights 字典 |
| P1-9 | **C2 FK/IK** | P95 尾部 | 检查是否奇异位形集中，加奇异检测过滤再报告；论文如实报尾部 |
| P1-10 | **A3 clean_and_visibility `imgs[:200]`** | 硬编码 200 张 | 去掉限制跑全量（先确认磁盘） |

### P1 — 仓库与文档同步（2 天内，可委托）

| # | 事项 |
|---|---|
| P1-11 | README 徽章指向 `offshore-wind-uav-arm/offshore-wind-uav-arm`（错）→ 改 `Atlas-did/SeaBreeze-Inspector`；README L51 端口 8800 → 8811 |
| P1-12 | 删除两条废弃 copilot 分支；`w1` 改名 `thesis-evidence` 或并入 main（w1 = main + 2 提交，无分叉） |
| P1-13 | 证据包已有 research_README.md + EVIDENCE_MAP.md，但**仓库 w1 的 research/ 里没有** → 补进仓库 |
| P1-14 | docs/SOFTWARE.md 引用已删除的 safety_guard.py / communication.py → 更新架构图 |
| P1-15 | 项目总结报告.md 失效引用：TELLO_PAYLOAD_DECISION.md、seabreeze_v3.pt/best.pt/v2_quick.pt、runs/detect/runs/train/...、data/raw/train/images 全部不存在 → 更新；全篇 0.58 同步 v4；端口 8800→8811 |

---

## 2. 行动路线（26 天倒排，按依赖排序）

### Phase 0 · 止血（8/19 – 8/21）—— 先让论文没有硬伤

> 此阶段不需要 GPU、不需要硬件，我（ClawsGO）可以直接代做。

- [ ] **T0-1** main.tex 三处泄漏数字替换为 v4 检查点数字（0.668/0.348/0.795/0.619），附「峰值 0.674@ep190」脚注 —— 需要你确认数字口径（见决策 D1）
- [ ] **T0-2** 全文 YOLOv8-Nano → YOLO11s；类别口径统一为「二分类 defect（由三缺陷类合并）」并保留三类的原始标注描述
- [ ] **T0-3** 补 §5 检测实验小节（数据→训练协议→指标→曲线/混淆矩阵→与 v2 对比表），接入 4 张孤儿图
- [ ] **T0-4** §6.2.4「test 划分为空」改为 v4 实情（test 480 张，零 parent 泄漏）；「FK/IK <1e-9」补 P95 尾部说明
- [ ] **T0-5** 论文/报告的「6 级抗风」措辞全部降级为「设计假设下 ~8 m/s」（或标"待实测验证"）
- [ ] **T0-6** 仓库清理：删 copilot 分支、w1 改名、README 徽章/端口、SOFTWARE.md、总结报告失效引用（P1-11~15 一批做掉）

**验收**：main.tex 中 grep 不到 `0.58`、`0.015`、`YOLOv8-Nano`、`6 级`；论文可编译且图全被引用。

### Phase 1 · 数据与证据闭环（8/19 – 8/24）—— 让每个数字可复现

- [ ] **T1-1** clean_manifest.csv 进仓库 data/（替换泄漏版；旧文件标注清楚）；三向零碰撞审计脚本入库
- [ ] **T1-2** 补建 v4 DTU 数据集 manifest（逐瓦片 sha256 + parent 图 + split）—— **需要你在训练机上跑一遍生成脚本**（数据不在本环境）
- [ ] **T1-3** B1 EKF 消融脚本改造 + 重跑，产出有区分度的报告（我改脚本，你确认数值）
- [ ] **T1-4** B2 敏感性排序 bug 修复（print 用 sorted）；等你的真实平台参数重跑
- [ ] **T1-5** A2 train_cli 死参数清理
- [ ] **T1-6** C2 FK/IK 奇异位形检查 + 论文如实报尾部

**验收**：EVIDENCE_MAP.md 里所有 ❌/⚠️ 都变成 ✅ 或明确"需数据/GPU"；仓库 data/ 只有干净 manifest。

### Phase 2 · 补实验（8/22 – 9/5）—— 与 Phase 1 部分并行，依赖 GPU/硬件

| 任务 | 依赖 | 负责人假设 | 产出 |
|---|---|---|---|
| **T2-1** A3 雾方案 B 训练跑通（特征一致性，检测任务只留 L_feat） | GPU（张睿服务器）+ 全量雾配对数据 | ClawsGO 写代码，你跑 | 雾浓度-精度衰减曲线 + 消融表 |
| **T2-2** B3 悬停风扇实测（多风况） | 风扇 + 风速仪 + 俯视相机 | 你 | 相对漂移抑制报告（措辞守住"相对"） |
| **T2-3** C1 SG90 台架 + 尺/录像实测 | 台架 | 你 | 末端物理精度（哪怕 ±5mm） |
| **T2-4** D2 实机时延插桩 | Tello/Arduino 实机 | 你 + ClawsGO 脚本 | t0–t3 分解报告 |
| **T2-5** v4 检测补强：加 defect 数据（把 Kimi 筛出的假 clean 重新标注缺陷） | 标注人力 + GPU | 你 | 若能从 423→700+ 张 defect，mAP 可再涨 |

> **T2-5 是 mAP 天花板的唯一解**（GAP_TRACKER §1.5.4 原文：想再涨唯一的路是加 defect 数据）。若人力不足，论文如实写天花板原因。

### Phase 3 · 论文整合与交付（9/5 – 9/15）

- [ ] **T3-1** §5 实验章节扩充（检测 v4 + 抗风实测 + 时延 + 雾衰减曲线），与 EVIDENCE_MAP 逐条对应
- [ ] **T3-2** 讨论/局限性/摘要按新数字重写；研究背景逻辑重构（按攻坚方案 §七：行业痛点 → 两类研究不足 → 本文方案）
- [ ] **T3-3** 参考文献核验：近 5 年、逐条 BibTeX、**禁用 AI 编造**、截图比对 PDF 渲染位置
- [ ] **T3-4** 预答辩演示：小型机全链路演示（起飞→绕塔→悬停巡检→返航）+ 论文 + 数据包
- [ ] **T3-5** 9/15 硬交付：Overleaf 编译版 + 演示视频 + 数据包（manifest/脚本/权重/报告四件套）

---

## 3. 数据与数字速查表（写论文时从这里取数，别从 00_说明 复制）

### v4 检测（唯一可信数字，零泄漏）

| 口径 | mAP50 | mAP50-95 | Precision | Recall |
|---|---|---|---|---|
| **最佳检查点 @ep182（推荐引用）** | **0.668** | **0.348** | **0.795** | **0.619** |
| 峰值 @ep190（脚注/讨论） | 0.674 | — | 0.756 | 0.664 |
| v2 对照（640 小目标） | 0.242 | 0.105 | 0.394 | 0.29 |

数据集 `clean_binary_v4_quick`：train 2120（defect 423 + clean 1697）/ val 440（82+358）/ test 480（82+398）。模型 **YOLO11s**，imgsz=1024，200 epochs，seed=0，epoch 182 按 mAP50-95 选。**架构混淆提醒**：v2 是 YOLOv8s，v4 是 YOLO11s——提升归因是「架构+分辨率+数据」的组合，论文必须写 attribution caveat（v4 报告 §2 已有）。

### 数据集清单（答辩证据）

| 文件 | 内容 | 状态 |
|---|---|---|
| `clean_manifest.csv` | 7001 唯一内容，train 4900/val 1050/test 1051，**三向零碰撞** | ✅ 干净版（但没进仓库） |
| `split_manifest.csv` | ILIAD 15528 行，仅 7001 唯一，val 90.8% 泄漏 | ❌ 泄漏版（仓库现状，别引用） |
| `data_sources.csv` | ILIAD 出处 + CC BY 4.0 + 重标注/增广说明 | ✅ |
| v4 DTU manifest | **未建** | ⚠️ 必须补 |

### 反例数字（写论文时只能这么写）

| 声明 | 现状 | 论文允许措辞 |
|---|---|---|
| 抗风等级 | 设计假设下 vmax≈8.2 m/s（5–95%：[7.0, 9.5]），12m/s 正裕度 0% | 「设计假设下抗风极限约 8 m/s；6 级风作业能力需工业平台实测验证」 |
| EKF 消融 | 三档 Q RMSE 无区分度 | 修脚本前**不写**消融结论；或只报「静风基线 d_hat std=0.109（残差来源见讨论）」 |
| FK/IK | P50=2.7e-10 mm，P95=123 mm | 「典型位形残差 <1e-9 m（P50）；奇异位形附近 P95 达 10² mm 量级」 |
| 闭环时延 | 纯模拟 | 实机插桩前**不报**具体数字 |

---

## 4. 需要你拍板的决策点

| # | 决策 | 选项 | 我的建议 |
|---|---|---|---|
| **D1** | v4 数字口径 | A. 只引最佳检查点（0.668…）B. 引峰值（0.674…）C. 两者都写 | **A + 脚注 C**：主表用检查点，脚注注明峰值——审稿人查 results.csv 对得上 |
| **D2** | 论文检测口径 | A. 二分类为主（推荐）B. 三分类为主 C. 都报 | **A**：v4 就是二分类，混淆矩阵也是二分类的；三分类作为「原始标注」在数据节描述 |
| **D3** | 是否投入 A3 雾方案 | A. 投入（需 GPU + 全量雾配对）B. 砍掉，论文只做方案对比表（§4.6 已有） | 看算力：**有 GPU 就做**（这是论文差异化的加分点），没有就把 §4.6 定位为"设计对比"而非实验 |
| **D4** | B1 消融改造投入 | A. 改脚本做有区分度的消融 B. 论文直接降级为定性 | **A**：成本低（纯本地 CPU），且能保住「自适应 Q 应对阵风」这个核心卖点 |
| **D5** | w1 分支去向 | A. 并回 main B. 改名 thesis-evidence 保留 | **A 或 B 都行，但必须二选一并写进 README**；当前 w1 无分叉历史，A 零冲突 |
| **D6** | 真实平台参数 | 你有 x500/Tello 实测的质量、连续推力、最大倾角、阻力系数吗？ | 有就发我，B2 立刻重跑；没有就全线走降级措辞 |

---

## 5. 我（ClawsGO）现在就能做的 vs 需要你的

**现在就能做（无需任何阻塞项）：**
- T0-1~T0-6 论文/仓库止血（等你确认 D1、D2 后动手改 main.tex 等）
- T1-1 clean_manifest 入库 + 泄漏审计脚本
- T1-3 B1 消融脚本改造（改完你在本地或我这边重跑验证）
- T1-4 B2 敏感性排序 bug 修复
- T1-5 A2 死参数清理
- T1-6 C2 FK/IK 尾部分析
- research_README.md / EVIDENCE_MAP.md 补进仓库

**需要你的：**
- D1/D2/D5 拍板（给个一句话即可）
- v4 DTU 数据集 manifest 生成（数据在训练机上）
- B2 真实平台参数（D6）
- T2-2/T2-3/T2-4 硬件实测（风扇/台架/实机插桩）
- T2-1/T2-5 GPU 训练（张睿服务器）
- 论文 Overleaf 编译验证（我改完 tex 你可以直接粘贴进 Overleaf）

---

*本计划基于四包材料与仓库实测交叉核对生成。所有数字均有出处（results.csv / manifest / report json 实测），所有"反例"均已本机运行验证。*
